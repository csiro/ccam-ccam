c dimension parameters for sscam2, now also includes what was in Ewa's 
      parameter (
     &  npmax  = ifull,      ! max patches in all gridcells
     &  npcell = 1,          ! max patches per gridcell
     &  npara0 = 25,         ! number of GLOBAL (param0) params
     &  npara1 = 40,         ! number of DEFAULT LOCAL (param1) params
     &  npara2 = 4,          ! number of PATCH (param2) params
     &  noutva = 55,         ! number of returned output variables
     &  nstore = 1,          ! number of returned store variables
     &  nhold  = 10)         ! number of returned time-indept vars

